package com.qma.auth.dto;

/**
 * @deprecated OAuth 2.0 was removed from this project.
 *             This file can be safely deleted from the filesystem.
 *             It is not referenced anywhere and has no runtime effect.
 */
@Deprecated
public class GoogleAuthRequest {
    // Intentionally empty — file retained only because the filesystem
    // does not support deletion through the current tooling.
    // DELETE this file manually.
}
